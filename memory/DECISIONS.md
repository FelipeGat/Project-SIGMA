# Log de decisões

Registro cronológico e leve de decisões do projeto — inclusive as que não justificam uma ADR própria. Decisões arquiteturais formais têm entrada aqui apontando para a ADR correspondente.

## 2026-08-04

- **SIGMA nasce como repositório próprio, desacoplado dos sistemas que orquestra.** → [ADR-0001](../docs/adr/0001-repositorio-proprio-e-independente.md)
- **Monorepo** (`backend/`, `frontend-web/`, `frontend-mobile/`, `docs/`) em vez de polyrepo, por ora. → [ADR-0002](../docs/adr/0002-estrutura-de-monorepo.md)
- **Mission é o agregado raiz** do domínio; toda funcionalidade se conecta ao seu ciclo de vida. → [ADR-0003](../docs/adr/0003-mission-como-entidade-central.md)
- **Três camadas de execução**: IA (provedor) → Agent (persona) → Skill (capacidade). → [ADR-0004](../docs/adr/0004-tres-camadas-ia-agente-skill.md)
- **SIGMA nunca executa diretamente**, apenas orquestra via Agent → Skill. → [ADR-0005](../docs/adr/0005-sigma-nunca-executa-diretamente.md)
- **Toda integração externa é uma Skill**, com contrato único (Config/Permissões/Entrada/Saída/Eventos/Logs/Testes/Docs). → [ADR-0006](../docs/adr/0006-integracao-externa-e-sempre-uma-skill.md)
- **Proibido acesso direto a banco de outro sistema** — só via API. → [ADR-0007](../docs/adr/0007-comunicacao-somente-via-api.md)
- **Arquitetura orientada a eventos**, Redis como backbone. → [ADR-0008](../docs/adr/0008-arquitetura-orientada-a-eventos.md)
- **Stack de referência**: Laravel 12/PHP 8.4 (backend), React/TS/Vite PWA (web), React Native/Expo (mobile). → [ADR-0009](../docs/adr/0009-stack-tecnologica-de-referencia.md)
- **Desenvolvimento por épico único, com aprovação obrigatória antes de código.** → [ADR-0010](../docs/adr/0010-processo-por-epicos-com-aprovacao.md)
- Sprint 0 (Foundation) entregue: README, VISION, ARCHITECTURE, ROADMAP, ADR-0001–0010, CONTRIBUTING, CODE_OF_CONDUCT, LICENSE, estrutura de diretórios. Commitado localmente, push não autorizado ainda.
- **Revisão (Sprint 0.1)**, a pedido do responsável do projeto, atuando como revisor externo (papel de CTO):
  - Repositório renomeado de `Sigma-IO` para `project-sigma` — o nome anterior sugeria escopo de I/O, menor que a ambição real da plataforma. O repositório GitHub remoto (`FelipeGat/Sigma-IO`) segue com o nome antigo até ser renomeado manualmente ou até o primeiro push, quando essa ação será revisitada.
  - Arquitetura reestruturada de um "Mission Engine" único para **9 Engines especializados**: Kernel, Intent Engine, Planner Engine, Mission Engine, Memory Engine, Agent Engine, Skill Engine, Execution Engine, Audit Engine. → [ADR-0011](../docs/adr/0011-arquitetura-em-camadas-de-engines.md)
  - **O Planner Engine decide o plano — nunca a IA/Agent.** Refinamento explícito de [ADR-0005](../docs/adr/0005-sigma-nunca-executa-diretamente.md). → [ADR-0012](../docs/adr/0012-planner-decide-nunca-a-ia.md)
  - **Intent Engine** isolado como porta de entrada de linguagem natural, antes do Planner. → [ADR-0013](../docs/adr/0013-intent-engine-como-porta-de-entrada.md)
  - **SIGMA é um Sistema Operacional, não uma IA** — nomenclatura dos módulos centrais padronizada com sufixo "Engine" para reforçar isso na documentação e, mais adiante, no código. → [ADR-0014](../docs/adr/0014-sigma-e-um-sistema-operacional-nao-uma-ia.md)
  - Roadmap reestruturado de lista de épicos por funcionalidade para **sequência por camada de Engine** (Foundation → Kernel → Intent → Planner → Mission → Memory → Agent → Skill → Execution → Audit → Interfaces → Automation → Analytics), mantendo o princípio de um épico por vez. → [ADR-0015](../docs/adr/0015-roadmap-por-camadas-nao-por-feature.md)
  - Documentação de filosofia (`MANIFESTO.md`), produto (`PRODUCT.md`), horizonte de longo prazo (`VISION_2030.md`) e glossário de domínio (`DOMAIN.md`) adicionada.
  - Pastas `agents/`, `skills/`, `knowledge/`, `playbooks/`, `memory/` criadas na raiz do repositório — documentação/conhecimento, sem código.
  - Push ao GitHub segue **não autorizado** até esta revisão ser aprovada.
- **Sprint 0 e Sprint 0.1 aprovadas** pelo Product Owner. Antes do primeiro push, solicitada uma **Sprint 0.2 — Organização Estrutural**, com uma segunda rodada de revisão externa (papel de CTO):
  - **Monorepo reestruturado** de `backend/`/`frontend-web/`/`frontend-mobile/` para `apps/`, `packages/`, `services/`, `plugins/`, `tools/`, `docker/` — estrutura "estilo Google", pensada para dezenas de repositórios em 2 anos. → [ADR-0016](../docs/adr/0016-monorepo-apps-packages-services.md)
  - **Plugin System**: Skills deixam de ser classes compiladas e passam a ser Plugins carregados dinamicamente via `manifest.json` — o Kernel nunca conhece a implementação concreta. → [ADR-0017](../docs/adr/0017-plugin-system.md)
  - **"Tudo é Evento"** oficializado como filosofia central, com catálogo canônico de eventos do fluxo de Mission. → [ADR-0018](../docs/adr/0018-tudo-e-evento.md)
  - **Observabilidade desde o dia zero** (Logs, Metrics, Tracing, Audit) — não uma melhoria futura. → [ADR-0019](../docs/adr/0019-observabilidade-desde-o-dia-zero.md)
  - **Workspace** introduzido como unidade de contexto operacional (agrega Client/Project/Budget/Meeting automaticamente), substituindo Project como âncora de contexto de uma Mission. → [ADR-0020](../docs/adr/0020-workspace-como-unidade-de-contexto.md)
  - **Multiempresa desde o schema** — hierarquia Tenant → Company → Workspace → User → Role, nunca retrofitada. → [ADR-0021](../docs/adr/0021-multitenancy-desde-o-schema.md)
  - **Memory em três níveis** — Operational, Project, Long Term — com promoção por repetição/generalização, nunca automática. → [ADR-0022](../docs/adr/0022-memory-em-tres-niveis.md)
  - **Governança formalizada em `/council`** (Product Owner=Felipe, CTO=ChatGPT, Lead Engineer=Claude, Creative=Gemini, Documentation=Manus) — distinto de `/agents` (execução runtime). → [ADR-0023](../docs/adr/0023-governanca-via-council.md)
  - Documentos novos: `KERNEL.md`, `PLUGIN_SYSTEM.md`, `EVENT_MODEL.md`, `TELEMETRY.md`, `WORKSPACES.md`, `MULTITENANCY.md`, `MEMORY_ARCHITECTURE.md`.
  - `execution-engine` foi acrescentado a `packages/` por consistência com os nove Engines de [ADR-0011](../docs/adr/0011-arquitetura-em-camadas-de-engines.md), mesmo não constando na lista original da revisão — sinalizado explicitamente, não uma correção silenciosa.
  - `skills/whatsapp.md` e `plugins/whatsapp/` adicionados (WhatsApp Cloud API, já em produção no Gestor.Alfa) para manter 1:1 entre Skills documentadas e Plugins com manifest.
  - **Repositório GitHub renomeado pelo Product Owner** para `FelipeGat/Project-SIGMA` — `origin` local atualizado. Criação de Organização no GitHub (`Alfa-Solucoes`/`Grupo-Solucoes`) sugerida, mas não executada — ação de conta fora do escopo do que posso executar diretamente.
  - Push ao GitHub segue **não autorizado** até esta Sprint 0.2 ser aprovada.
- **Sprint 0, 0.1 e 0.2 aprovadas oficialmente pelo Product Owner. Primeiro `git push` executado** — `github.com/FelipeGat/Project-SIGMA`, branch `main`, 3 commits. Nascimento oficial do Project SIGMA como produto, não mais experimental.
- **"Sprint" substituído por "Release"** como unidade de entrega; Sprints 0/0.1/0.2 reconhecidas retroativamente como Release 0 — Foundation, sem renomear a história. → [ADR-0024](../docs/adr/0024-terminologia-release.md)
- **Maior decisão do projeto até aqui**: a Release 2 (Kernel) é adiada. **Release 1 é o SIGMA Protocol** — especificação de como todo Engine, Plugin e Agent conversa, antes de qualquer código. → [ADR-0025](../docs/adr/0025-protocol-antecede-kernel.md)
  - Sinalizado ao Product Owner: a nova ordem de Releases coloca **Planner Engine (5) antes do Intent Engine (6)**, o que parece inverter a dependência já registrada em ADR-0012/0013 (Planner recebe uma Intent já pronta). Registrado como nota de transparência na ADR-0025, aguardando confirmação — não assumido silenciosamente.
- **Envelope de resposta padronizado** para toda resposta do SIGMA (Skill, Plugin, Agent, canal externo): `success`, `mission`, `workspace`, `events`, `memory`, `nextActions`, `logs`. Acrescentei `data` e `error`, sinalizado explicitamente — sem eles o envelope não carregaria o resultado de negócio nem um erro estruturado. → [ADR-0026](../docs/adr/0026-envelope-de-resposta-padronizado.md)
- **Capability** introduzida como unidade de implementação de uma Skill — não função solta. Campo `actions` do manifest de Plugin renomeado para `capabilities`; os 6 manifests já existentes e o schema foram migrados nesta mesma revisão. → [ADR-0027](../docs/adr/0027-capability-unidade-de-skill.md)
- **SIGMA executa Intenções, não comandos** — DNA do sistema. Uma Intent pode decompor em múltiplas Missions relacionadas (cardinalidade 1:N, antes 1:1). `DOMAIN.md` e o diagrama de relação entre camadas atualizados. → [ADR-0028](../docs/adr/0028-intencao-nao-comando.md)
- **Princípio da Autonomia Progressiva** — 4 níveis (Consultivo/Assistido/Delegado/Operacional), configuráveis por User/Role e exigidos por Capability. Substitui o booleano `requires_human_approval` por `autonomy_level_required` (0–3) em cada Capability. → [ADR-0029](../docs/adr/0029-autonomia-progressiva.md)
- `SIGMA_PROTOCOL.md` criado — documento de maior autoridade técnica do projeto, acima de ARCHITECTURE.md em decisões de formato/contrato.
- Push da Release 1 segue **não realizado** até esta revisão ser aprovada.
- **Revisão da Release 1 (nota 10/10) — aprovada com adições**, todas incorporadas nesta mesma entrega antes do push:
  - **Envelope v2**: `protocolVersion`, `correlationId`, `requestId`, `timestamp`, `actor`, `intent`, `capability`, `warnings`, `audit`; `mission`/`workspace` renomeados para `missionId`/`workspaceId`; `logs` removido (substituído por `correlationId` + consulta ao Audit Engine). → [ADR-0030](../docs/adr/0030-envelope-v2.md)
  - **Ordem de Runtime vs. Ordem de Desenvolvimento** oficializadas como conceitos distintos, fechando a tensão sinalizada no ADR-0025 — a ordem de Releases (Protocol→Kernel→Memory→Mission→Planner→Intent) já estava correta, só faltava nomear a distinção. → [ADR-0031](../docs/adr/0031-ordem-runtime-vs-desenvolvimento.md)
  - **SGL — SIGMA Language** criada: gramática intermediária para representar Intent/Mission antes do JSON. → [SGL.md](../SGL.md), [ADR-0032](../docs/adr/0032-sigma-language.md)
  - **Capability Registry**: `version`, `owner`, `dependencies` acrescentados a cada Capability; os 6 manifests migrados. → [ADR-0033](../docs/adr/0033-capability-registry.md)
  - **Eventos em três camadas** — Technical, Semantic, Business. → [ADR-0034](../docs/adr/0034-eventos-tres-camadas.md)
  - **Digital Twin**: SIGMA nunca lê um sistema externo direto, sempre via representação viva (Client/Project/Company/User). → [DIGITAL_TWIN.md](../DIGITAL_TWIN.md), [ADR-0035](../docs/adr/0035-digital-twin.md)
  - **"Objetivo" tratado como o campo `objective` da Intent**, não uma camada nova — proposto, não confirmado, por haver ambiguidade real entre o exemplo dado (Objetivo→Missões→Tarefas→Eventos) e a especificação de SGL (onde `objective` já é campo de `INTENT`). Sinalizado explicitamente. → [ADR-0036](../docs/adr/0036-objetivo-e-campo-da-intent.md)
  - **Declarativo, não Imperativo** oficializado como princípio do MANIFESTO.md — uma Intent descreve estado desejado, nunca passo a passo. → [ADR-0037](../docs/adr/0037-declarativo-nao-imperativo.md)
- **Release 1 aprovada oficialmente. Push autorizado.** Project SIGMA oficialmente iniciado como produto (não mais experimental).
- **Release 2 renomeada de "Kernel" para "SIGMA Bootstrap"**, escopo reduzido a infraestrutura pura (Config/Logger/DI/Container/Modules/Events/Lifecycle/Health) — nada de Mission, IA ou Plugins. → [ADR-0038](../docs/adr/0038-sigma-bootstrap-nao-kernel-completo.md), [BOOTSTRAP.md](../BOOTSTRAP.md)
  - Consequência sinalizada: o schema de multiempresa, antes bundlado na Release 2, ficou sem casa definitiva — proposto mover para a Release 3, a confirmar.
- Autorizado preparar a proposta formal da Release 2 (Objetivo/Escopo/Arquitetura/Dependências/Riscos/Entregáveis/Testes/Critérios de Aceite) — código só começa após essa proposta ser revisada e aprovada.
- **Revisão da proposta da Release 2 (nota 9,8/10) — aprovada com alteração obrigatória.** "Pensar como criadores de um sistema operacional, não como arquitetos de software":
  - **Identity Engine criado como Release própria (Release 3)**, extraído do que seria Memory Engine — Identity responde "quem", Memory responde "o que sei". Resolve definitivamente a pendência de onde vive o schema de multiempresa (Tenant/Company/Workspace/User/Role): nem Bootstrap, nem Memory — Identity Engine. → [ADR-0039](../docs/adr/0039-identity-engine.md)
  - Releases 4–14 renumeradas (+1) para acomodar o Identity Engine — mudança mecânica em todo o repositório.
  - **Bootstrap nunca conhece Engine — apenas Module.** Engine/Plugin/Service/Package são valores de `kind`, metadado sem efeito no comportamento do Kernel. KERNEL.md e BOOTSTRAP.md reescritos. → [ADR-0040](../docs/adr/0040-bootstrap-nao-conhece-engines.md)
  - **Lifecycle estendido**: `discover → register → boot → start → ready → degraded → shutdown` — o estado `degraded` permite falha isolada de um Module (ex: Telegram fora do ar) sem derrubar o sistema. → [ADR-0041](../docs/adr/0041-lifecycle-estendido.md)
  - **Health estilo Kubernetes**: `/health/live`, `/health/ready`, `/health/startup`, substituindo um único `/health`. → [ADR-0042](../docs/adr/0042-health-estilo-kubernetes.md)
  - **Telemetry desde o Bootstrap**, não só um Logger — os 4 pilares (Logs/Metrics/Tracing/Audit) ativos desde o primeiro `boot`. → [ADR-0043](../docs/adr/0043-telemetry-desde-o-bootstrap.md)
  - **Configuration Provider** (estilo Spring Boot) — cada Module declara seu próprio schema de configuração; nenhum Module lê variável de ambiente diretamente. → [ADR-0044](../docs/adr/0044-configuration-provider.md)
  - **System Manifest**: um único arquivo (`system-manifest.yaml`) que o Bootstrap lê na subida — `engines`, `plugins`, `providers`, `workspace` — tudo o mais é descoberto. → [SYSTEM_MANIFEST.md](../SYSTEM_MANIFEST.md), [ADR-0045](../docs/adr/0045-system-manifest.md)
  - **Self-Describing Components** — "a maior sugestão até agora" do Product Owner: todo Module (Engine, Plugin, Service, Agent) responde `describe()` — id, type, version, status, capabilities, dependencies, health. Base para diagrama de arquitetura automático, validação de dependência antes do boot, busca por Capability, futura UI de administração. → [ADR-0046](../docs/adr/0046-self-describing-components.md)
  - **Toda Release passa a produzir dois artefatos: Código e Decision Log** — decisões locais tomadas dentro do escopo já aprovado, registradas ao final da implementação, distintas de ADR (que é anterior ao código e de escopo mais amplo). → [ADR-0047](../docs/adr/0047-decision-log-por-release.md), processo formalizado em `CONTRIBUTING.md`
  - `docs/architecture/ARCHITECTURE.md` atualizado: 9 → 10 Engines.
  - Proposta da Release 2 reescrita (revisão 2) incorporando tudo acima — aguardando confirmação final do Product Owner antes do primeiro código.
- **"Ela não é mais apenas Lead Engineer, passou a atuar como Software Architect."** Aprovação final da Release 2 (revisão 3), com processo permanente mudando a partir daqui:
  - **Processo de 4 fases**: Proposal → Architecture Review → Implementation → Validation. → [ADR-0048](../docs/adr/0048-processo-quatro-fases.md)
  - **Sigma Contracts** (`contracts/*.contract.yaml`) — contrato formal por conceito de domínio (entrada/saída/eventos/permissões/versões). Nenhum contrato ainda existe; nascem com a Release do conceito correspondente. → [ADR-0049](../docs/adr/0049-sigma-contracts.md)
  - **COMPATIBILITY.md** — matriz Kernel × Protocol × Plugin API × Status. → [ADR-0050](../docs/adr/0050-compatibility-matrix.md)
  - **RFC** (`docs/rfc/`) — ideias antes da decisão: RFC → Discussão → Aprovação → ADR → Código. → [ADR-0051](../docs/adr/0051-processo-rfc.md)
  - **Kernel API apenas por interface** — `ILogger`, `IEventBus`, `IModule`, `IConfiguration`, `IHealth`, `IContainer`; nunca classe concreta. → [ADR-0052](../docs/adr/0052-kernel-api-apenas-interfaces.md)
  - **Escopo da Release 2 restrito explicitamente**, incluindo remoção de banco de dados (MariaDB sai do docker-compose desta Release — nada a persistir ainda). → [ADR-0053](../docs/adr/0053-escopo-restrito-release-2.md)
  - **Três níveis de validação obrigatórios**: Testes Automatizados, Architecture Validation, Scenario Validation — com cenários explícitos para o Bootstrap (sobe sem módulos, módulo opcional ausente, módulo degraded, Manifest com erro, módulo incompatível rejeitado, health correto). → [ADR-0054](../docs/adr/0054-tres-niveis-de-validacao.md)
  - **`/sdk` multi-linguagem** (php/typescript/python/docs) na raiz, distinto de `packages/sdk` (interno). → [ADR-0055](../docs/adr/0055-sdk-multi-linguagem.md)
  - Proposta da Release 2 reescrita pela terceira vez (revisão 3) com tudo isso incorporado.
- **✅ Release 2 aprovada oficialmente. ✅ Push dos commits d1478ee e ff6ee3e autorizado (já realizado). ✅ Primeira linha de código do Project SIGMA autorizada.**
- **Release 2 — SIGMA Bootstrap implementada.** `packages/core` (Id, SigmaException — 4 testes), `packages/kernel` (6 interfaces, Container, Logger, ConfigurationProvider, HealthManager, LifecycleManager, SystemManifestLoader, KernelModule — 30 testes), `services/event-bus` (RedisEventBus, EventBusModule — 6 testes), `services/gateway` (Bootstrap, HealthEndpoints, Envelope, front controller — 8 testes). **48 testes automatizados, todos passando.** Validado com HTTP real via `php -S` (`/health/live`, `/health/ready`, `/health/startup`, 404, manifest ausente).
  - `contracts/Module.contract.yaml` publicado — primeiro Sigma Contract real.
  - Achado real durante Scenario Validation: `$_ENV`/`$_SERVER` não confiáveis para ler ambiente sob o servidor embutido do PHP nesta instalação (XAMPP) — corrigido para `getenv()`. Exatamente o tipo de problema que a Scenario Validation existe para pegar.
  - Sinalizado, não resolvido: PHP local é 8.2 (ADR-0009 pede 8.4); `docker-compose`/Dockerfile escritos mas build não verificado (Docker Desktop indisponível na sessão); listener Redis cross-processo real ainda não implementado (nenhum consumidor existe nesta Release).
  - Decision Log completo em [docs/releases/0002-sigma-bootstrap-decision-log.md](../docs/releases/0002-sigma-bootstrap-decision-log.md).
  - Push do código **não realizado** — aguardando confirmação explícita, distinta da aprovação já dada para os commits de documentação.
- **Release 2 aprovada com ressalvas menores (nota 9.7/10). ✅ Push do commit `87053ed` autorizado (realizado).** Decisão explícita de **não** reconciliar PHP 8.2→8.4 agora (fica para a Release de CI/CD) e **não** validar Docker agora (aceito, desde que registrado — já estava). Antes da Release 3, exatamente quatro entregas pedidas:
  - **EventBus por composição**: `packages/kernel` ganha `InMemoryEventBus` (pub/sub local puro, sem Redis); `services/event-bus`'s `RedisEventBus` passa a compor `InMemoryEventBus` para entrega local em vez de reimplementar o mapa de handlers — `IEventBus` continua a única interface (já seguia a convenção `I`-prefix do Kernel API, não precisou renomear). → [ADR-0057](../docs/adr/0057-eventbus-composicao-inmemory.md)
  - **`VALIDATION_REPORT.md` como artefato obrigatório de toda Release**, distinto do Decision Log (prova de execução, não explicação de decisão) — estrutura fixa: Release/Ambiente/PHP/Docker/HTTP/Testes/Coverage/Scenario Validation/Pendências. Template em `docs/releases/VALIDATION_REPORT.template.md`; aplicado retroativamente à Release 2. → [ADR-0056](../docs/adr/0056-validation-report-obrigatorio.md)
  - **`manifestVersion` no System Manifest** — campo obrigatório, versiona o formato do arquivo (distinto de `version`, que é a versão do projeto). Loader rejeita ausência ou valor não suportado. → [ADR-0058](../docs/adr/0058-manifest-version.md)
  - **`IDENTITY_MODEL.md` publicado** — as dez entidades de identidade (Tenant/Company/Workspace/User/Team/Role/Permission/Autonomy Level/Context/Session) e as relações entre elas, incluindo duas entidades novas (Permission como chave string sempre concedida via Role, nunca direta a um User; Session como registro durável de autenticação, distinto de Context que é resolvido em runtime e nunca persistido). Decisão central: Permission nunca é atribuída diretamente a um User/Team, sempre via Role — para não deixar o modelo crescer de forma inconsistente.
  - **Proposta da Release 3 — Identity Engine** apresentada (revisão 1, arquivo renomeado depois para [docs/releases/0003a-identity-domain.md](../docs/releases/0003a-identity-domain.md) quando a Release foi dividida em 3A/3B) — primeiro `IModule` de domínio real, primeira Release com persistência (MariaDB). **Aguardando aprovação. Nenhuma linha de código do Identity será escrita antes de duas aprovações separadas: IDENTITY_MODEL.md e esta Proposal** — restrição explícita do Product Owner.
  - Observações registradas mas deliberadamente **não implementadas agora** (não estavam nas "quatro entregas" explícitas, apenas nas observações que as precederam) — ficam de referência para quando alguma Release futura tocar o assunto: endpoint `/health/details` (status/modules/version/uptime); `LogContext` (Workspace/Mission/CorrelationId/Module) para o Logger; validação de Bootstrap para Module com dependência declarada mas inexistente (marcar como FAILED, preparação para Dependency Graph futuro).
  - Suíte completa re-executada após as mudanças: 54 testes (48 + 6 novos: 4 de `InMemoryEventBus`, 2 de validação de `manifestVersion`), todos passando; validado também via HTTP real (`php -S`) contra o `system-manifest.yaml` já com `manifestVersion: 1`.
  - Link-check de todo o repositório (168 arquivos `.md`): 0 links quebrados.
- **Revisão da Pré-Release 3 aprovada (nota 9,9/10). ✅ Push do commit `a57fd7a` autorizado (realizado).** Elogiou explicitamente: disciplina de escopo (implementar só as quatro entregas, não as observações extras); Permission nunca ligada direto ao User, sempre via Role — "deve permanecer como princípio do SIGMA"; EventBus por composição em vez de herança (SOLID); Validation Report.
  - **Mudança obrigatória para a Release 3 seguir para implementação**: dois documentos, ambos entregues nesta rodada — [IDENTITY_LIFECYCLE.md](../IDENTITY_LIFECYCLE.md) (fluxo: Identity criada → Autenticada → Contexto carregado → Session criada → Workspace selecionado → Permissões resolvidas → Autonomia calculada → Identity pronta) e `contracts/Identity.contract.yaml` (contrato antes do código, mesmo padrão de `Module.contract.yaml`).
  - **Cinco refinamentos de direção aprovados sem reabrir a Proposal** — viram ADR durante a Implementation da Release 3, não antes: (1) `Identity` como objeto raiz (`Identity → User → Workspace → Permissions → Context → Autonomy`), substituindo `Context` isolado; (2) `Session` autentica uma `Identity`, não um `User` direto — prepara múltiplas Sessions concorrentes por pessoa (um Workspace por Session); (3) `Context` **imutável** — trocar de Workspace = nova Session, nunca mutação; (4) `Team` tipado — `System Team` (CTO/Developer/Support) vs. `Business Team` (Comercial/Financeiro/Obras); (5) `Autonomy` baseada em capability nomeada (`CanApproveBudget`, `CanDeleteMission`, `CanDeploy`) em vez de nível numérico 0-3 — revisita o esquema do ADR-0029 quando formalizado.
  - Release 3 — Proposal revisão 2, **status: aprovada para implementação**. `docs/releases/README.md` e `ROADMAP.md` atualizados.
  - **Orientação estratégica permanente do Product Owner, a partir da Release 3**: parar de perguntar "como o framework X faz" e passar a perguntar "como o SIGMA deve fazer" — momento em que a plataforma começa a ganhar identidade própria. Salvo como memória de colaboração de longo prazo (fora do repositório, no sistema de memória do Claude).
  - Observação do Product Owner sobre a trajetória do projeto: separação saudável entre Arquitetura/Infraestrutura/Domínio sendo mantida desde o início, ao contrário da maioria dos projetos que misturam essas camadas e pagam dívida técnica anos depois.
- **Revisão da Pré-Release 3 aprovada (nota 9,9/10). ✅ Push do commit `5ee6d18` autorizado (realizado).** "Essa é a primeira vez que sinto que o projeto deixou de ser uma coleção de documentos e passou a ter um processo de engenharia maduro."
  - **Regra nova, permanente**: "a memória oficial do SIGMA nunca pode depender da memória de uma IA" — repositório é sempre a fonte da verdade; memória de qualquer ferramenta de IA é cache de conveniência. → [ADR-0059](../docs/adr/0059-repositorio-e-fonte-da-verdade.md). Documentado também em `memory/README.md` do próprio repositório.
  - **Release 3 dividida em duas sub-Releases sequenciais**: **3A — Identity Domain** (Value Objects/Entities/Aggregates/Contracts/eventos/regras, sem persistência/banco/auth/API) e **3B — Identity Infrastructure** (persistência/repositórios/API/integração), 3B só começando depois de 3A implementada e validada. → [ADR-0060](../docs/adr/0060-release-dividida-em-sub-releases.md). `docs/releases/0003-identity-engine.md` renomeado para `0003a-identity-domain.md` (revisão 3, reescrita com escopo só de Domain) + `0003b-identity-infrastructure.md` criado como placeholder.
  - **Todo Engine passa a seguir quatro camadas DDD**: `Domain/Application/Infrastructure/Interface`, exemplo `packages/identity-engine/{Domain,Application,Infrastructure,Interface}`. → [ADR-0061](../docs/adr/0061-engine-quatro-camadas-ddd.md)
  - **Identity Engine nunca conhece outro Engine** (Memory/Mission/Planner/Agent/Skill/Execution/Audit) — comunicação só por eventos de domínio publicados, mais estrito que a regra geral de arquitetura orientada a eventos. → [ADR-0062](../docs/adr/0062-identity-nunca-conhece-outro-engine.md)
  - **Identificadores de domínio são Value Objects, nunca string primitiva** — `TenantId`, `CompanyId`, `WorkspaceId`, `UserId`, `TeamId`, `RoleId`, `PermissionId`, `SessionId`, `IdentityId`, a partir da Release 3 e para todo Engine futuro. → [ADR-0063](../docs/adr/0063-identificadores-como-value-objects.md)
  - **Entrega obrigatória entregue**: [DOMAIN_EVENTS.md](../DOMAIN_EVENTS.md) — catálogo dos dez eventos de domínio do Identity Engine (camada Semantic), incl. `RoleRevoked` adicionado por simetria com `RoleAssigned` (sinalizado como acréscimo, não estava na lista original).
  - Avaliação de maturidade do Product Owner: Visão 100%, Arquitetura 95%, Governança 95%, Processo 95%, Documentação 98%, Infraestrutura 60%, Código 15%, Funcionalidade de Negócio 2% — "parece pouco em código, mas na verdade é um excelente sinal. Investimos primeiro na fundação."
- **Release 3A — Identity Domain implementada.** Autorização: "Vamos seguir até o uso preciso seguir e ver ele rodando." Esclarecido antes de codar: 3A não tem servidor/API (isso é 3B) — "rodando" aqui é a suíte de testes provando o fluxo completo de IDENTITY_LIFECYCLE.md ponta a ponta em memória.
  - `packages/identity-engine/src/Domain/` — `Identifier` (base abstrata, late static binding) + 9 IDs; `Tenant`/`Company`/`Workspace`/`User`/`Team`(tipado)/`Permission`/`Scope`/`Role`(permissões + autonomy capabilities)/`RoleAssignment`/`Session`/`Context`(imutável)/`Identity`(agregado raiz); 10 eventos de domínio + `RecordsDomainEvents`. **Zero dependência de infraestrutura** — só `sigma/core`.
  - **50 testes automatizados, todos passando** (79 assertions) — cobrindo cada Value Object/entidade + o fluxo completo (Identity criada→ativada→autenticada→Workspace selecionado→Context resolvido) incluindo via Team, via Tenant-scope, com assignment revogado, com Session de outra Identity rejeitada. Suíte completa do monorepo: **104 testes**.
  - Achado real: `@dataProvider` em doc-comment gerava deprecation no PHPUnit 11 (removido no 12) — corrigido para `#[DataProvider(...)]` antes de considerar a suíte "verde" (zero warnings, não só zero falhas).
  - **Cinco ADRs de direção escritas como parte do código, conforme prometido**: [0064](../docs/adr/0064-identity-como-agregado-raiz.md) (Identity como agregado raiz — `IDENTITY_MODEL.md` fica desatualizado nesse ponto específico, não editado retroativamente, por princípio), [0065](../docs/adr/0065-session-autentica-identity.md) (Session→Identity), [0066](../docs/adr/0066-context-imutavel.md) (Context imutável — nuance: 1ª seleção de Workspace muta a mesma Session, 2ª exige Session nova), [0067](../docs/adr/0067-team-tipado.md) (Team tipado), [0068](../docs/adr/0068-autonomy-por-capability.md) (Autonomy por capability nomeada, união OR entre Roles aplicáveis — decisão de design não especificada literalmente, sinalizada como tal; divergência com `autonomy_level_required` numérico registrada, reconciliação adiada para o Skill Engine).
  - [Decision Log](../docs/releases/0003a-identity-domain-decision-log.md) e [Validation Report](../docs/releases/0003a-identity-domain-validation-report.md) publicados — Docker/HTTP marcados explicitamente "não aplicável a esta sub-Release", nunca omitidos.
  - Push **não realizado** — commit local aguardando confirmação, mesmo padrão de sempre.
- **Proposta da Release 3B — Identity Infrastructure escrita** ([docs/releases/0003b-identity-infrastructure.md](../docs/releases/0003b-identity-infrastructure.md), revisão 1), substituindo o placeholder — aguardando aprovação do Product Owner, nenhum código ainda. Decisões já tomadas na própria Proposal (deixaram de ser perguntas em aberto): camada de acesso a dados = PDO puro + runner de migration próprio, sem Doctrine DBAL (mantém o precedente framework-agnóstico de `packages/kernel`); filtro por Tenant vive dentro de cada método de repositório, nunca é responsabilidade de quem chama. `docker compose up --build` com MariaDB passa a ser Critério de Aceite obrigatório desta Release — não repetir a pendência da Release 2.
- **"sim pode seguir" — Release 3B implementada.** Autorização de push do commit anterior + luz verde para implementação. `packages/identity-engine` completo nas quatro camadas (Domain já existia da 3A; Application/Infrastructure/Interfaces novas), `services/auth` criado do zero.
  - `Envelope` movido de `services/gateway` para `packages/core` (novo compartilhamento entre serviços HTTP) → [ADR-0069](../docs/adr/0069-envelope-em-packages-core.md).
  - Cinco achados reais durante a implementação, todos corrigidos e documentados no Decision Log da 3B: `Identity`/`Session`/`RoleAssignment` precisaram de `reconstitute()` (hidratar sem redisparar evento); `Team` ganhou `members()`; `IdentityEngineModule` vazava `\PDOException` crua (corrigido para `SigmaException`); `AuthEndpoints` só capturava `SigmaException` (corrigido para `\Throwable`, evita quebrar o Envelope quando infraestrutura cai); `system-manifest.yaml` compartilhado por `gateway` e `auth` exigiu `identity-engine: optional: true` (gateway não registra esse Module).
  - `interface` é palavra reservada do PHP — a camada Interface usa pasta/namespace `Interfaces/` (plural), achado ao criar o primeiro arquivo.
  - **Docker Desktop ficou disponível no meio desta sessão** (indisponível desde a Release 2) — `docker compose up --build` executado de fato pela primeira vez no projeto: 4 containers (redis/mariadb/gateway/auth) subiram, fluxo completo login→select-workspace→context→logout validado via HTTP real, incluindo publicação real de evento no Redis. Portas do host remapeadas para não colidir com outro projeto Docker já rodando na máquina. Pilha derrubada ao final, sem deixar nada residente.
  - Antes do Docker ficar disponível, os testes de Infrastructure/`services-auth` foram validados contra o `mysqld` do XAMPP local (iniciado e parado deliberadamente só para essa validação, bancos de teste descartados).
  - **135 testes automatizados no monorepo, todos passando** (8+36+6+8+72+5).
  - [Decision Log](../docs/releases/0003b-identity-infrastructure-decision-log.md) e [Validation Report](../docs/releases/0003b-identity-infrastructure-validation-report.md) publicados — Docker e HTTP preenchidos de verdade, não "não aplicável" como na 3A.
  - Push **não realizado** — commit local aguardando confirmação.
- **Revisão da Release 3 aprovada (nota 9,95/10). ✅ Push do commit `13d8e0e` autorizado (realizado).** Elogiou: "testaram a arquitetura, não apenas o código" (login→workspace→context→logout); os cinco bugs reais encontrados; Envelope movido para `packages/core`; PDO puro em vez de Laravel/Doctrine.
  - **Cinco observações de arquitetura** (Identity independe de autenticação; PasswordHasher→CredentialProvider; Session como Aggregate próprio; Workspace/Context deveriam pertencer à Session, não à Identity; eventos precisam de metadata padrão) — nenhuma marcada "obrigatória".
  - **Decisão estratégica, na mesma mensagem**: não começar a Release 4 (Memory) ainda — criar **Release 3.5 — Architecture Consolidation** antes, sem mudar o produto: EVENT_CATALOG.md, VERSION.md por Engine, CHANGELOG.md do produto, validação cruzada de Contracts/ADRs/Decision Logs, revisão de testes por camada, teste completo `bootstrap→login→workspace→logout` em ambiente limpo, revisão da separação Identity/Session/CredentialProvider (sem mudar comportamento). "É muito mais barato consolidar agora do que corrigir depois da Mission, Planner e Agent."
  - **Release 4 (Memory Engine) declarada o segundo marco mais importante do projeto**, atrás só da Foundation — recomendação de investir tempo extra na modelagem, mesmo que atrase o cronograma.
- **Mensagem seguinte, ainda na mesma rodada**: análise estratégica completa do CTO/arquiteto de plataforma, pensando 5-10 anos à frente. **Roadmap estendido de 14 para 24 Releases** — "siga exatamente o que está aqui... não precisa pedir confirmação, apenas desenvolva; dúvidas, retiro com você."
  - Cinco Engines novos: 16 Knowledge Engine (cérebro documental), 17 Digital Twin (Engine própria, hoje só conceito), 18 Capability Registry (evolução do Plugin System), 19 Council Engine (colaboração multi-IA executável, "onde desaparece o Felipe intermediador"), 20 Multi-Agent Runtime (execução concorrente).
  - Release 12 nova: Gateway/API (empurra Interfaces 12→13, Automation 13→14, Analytics 14→15).
  - Releases 21-23 nomeadas sem escopo detalhado (Marketplace/Cloud Sync/Production Hardening); 24 = SIGMA v1.0.
  - Cinco componentes estruturais sinalizados sem Release própria: Scheduler, Secrets Manager, Cache Layer, Observability, Policy Engine.
  - **Dúvida sinalizada, não decidida silenciosamente**: a tabela recebida listava `6-Intent, 7-Planner`, invertendo a ordem já decidida deliberadamente em ADR-0031 (`6-Planner, 7-Intent`, Planner construído primeiro com Intents mockadas). Mantive a numeração do ADR-0031 e registrei a divergência explicitamente (ROADMAP.md + ADR-0070), aguardando confirmação.
  - [ADR-0070](../docs/adr/0070-roadmap-estendido-24-releases.md) formaliza tudo isso; referências cruzadas a "Release 12/13/14" em 10 arquivos (apps/web, apps/mobile, council/Creative, EVENT_MODEL, DOMAIN_EVENTS, design-system/README, TELEMETRY, ADR-0019, ADR-0034) corrigidas mecanicamente para a nova numeração.
  - **Release 3.5 executada nesta mesma rodada**: EVENT_CATALOG.md (catálogo de todo evento do SIGMA, complementa sem duplicar DOMAIN_EVENTS.md), `packages/identity-engine/VERSION.md` (primeiro Engine com SemVer formalizado), CHANGELOG.md (retroativo, linguagem de produto), `PasswordHasher`→`CredentialProvider` (renomeação pura, 135 testes continuam verdes), nove ADRs novas (0070-0078).
  - **Validação cruzada encontrou 2 divergências reais**, corrigidas: `contracts/Identity.contract.yaml` tinha `events: []` desatualizado (pré-código) e `output` tipado como `Identity` quando o valor real retornado é `Context`. 78 ADRs referenciadas no repo, 0 inexistentes (verificado por script); índice sem duplicata/lacuna.
  - **Achado real do teste em ambiente limpo** (`docker compose down -v` + `build --no-cache` + `up`): migrations do Identity Engine só rodam na primeira requisição HTTP (dentro de `boot()`), não há hook de inicialização de container — um seed via `docker exec` falhou antes da primeira requisição real. Documentado, não corrigido (mudaria comportamento).
  - Fluxo completo `login→workspace→context→logout` revalidado do zero, ambiente genuinamente limpo — sucesso.
  - [Decision Log](../docs/releases/0003.5-architecture-consolidation-decision-log.md) e [Validation Report](../docs/releases/0003.5-architecture-consolidation-validation-report.md) publicados.
- **"siga"** — push do commit `045fdd1` (Release 3.5) realizado. Início do trabalho de modelagem da Release 4 — Memory Engine, seguindo a recomendação do Product Owner de investir tempo extra ("segundo marco mais importante do projeto").
  - **[MEMORY_MODEL.md](../MEMORY_MODEL.md)** publicado — `MemoryRecord`/`KnowledgeRecord`/`DigitalTwin`, deixando claro que os três são conceitualmente distintos (factual-curado / experiencial / factual-volátil) apesar de compartilharem custodiante.
  - **[MEMORY_LIFECYCLE.md](../MEMORY_LIFECYCLE.md)** publicado — fluxo de promoção de Memory + sincronização de Digital Twin.
  - **Três decisões de fronteira resolvidas** (pesquisa prévia via subagente encontrou tensões reais entre documentos existentes, resolvidas explicitamente):
    - [ADR-0079](../docs/adr/0079-usertwin-desde-a-release-4.md): `UserTwin` populado desde já (eventos do Identity Engine já são reais), demais Twins esperam a Release 8 — reconcilia uma contradição real entre `ROADMAP.md` ("custódia dos primeiros Digital Twins" na Release 4) e `DIGITAL_TWIN.md` (primeiro Twin real só na Release 8).
    - [ADR-0080](../docs/adr/0080-knowledge-release4-indice-simples.md): Knowledge da Release 4 é índice textual simples sobre `/knowledge`; busca semântica fica para a Release 16 — evita a Release 4 crescer para dentro do escopo da 16.
    - [ADR-0081](../docs/adr/0081-mecanica-de-promocao-de-memory.md): mecânica de promoção formalizada — `subjectKey` estável, repetição (mesmo Workspace, Missions diferentes) promove Operational→Project, generalização (Workspaces diferentes) promove Project→LongTerm, `promotedFrom` preserva proveniência sempre.
  - `contracts/Memory.contract.yaml` publicado — mesmo padrão de `Identity.contract.yaml` antes do código.
  - `DOMAIN_EVENTS.md`/`EVENT_CATALOG.md` ganharam seção "Memory Engine" — seis eventos catalogados antes do código, mesmo padrão de Identity antes da 3A. Ajustada a regra do "Processo" do EVENT_CATALOG.md, que dizia "evento nasce junto do código" — corrigida para refletir a prática real (catalogado junto da modelagem, antes do código, desde que a Release já tenha Proposal em preparação).
  - Correções de nomenclatura obsoleta encontradas durante a pesquisa: "Épico E5" (`knowledge/README.md`) e "Camada L5" (`packages/memory-engine/README.md`) → "Release 4".
  - **Release dividida em 4A (Domain) / 4B (Infrastructure)**, mesmo padrão da Release 3 ([ADR-0060](../docs/adr/0060-release-dividida-em-sub-releases.md)) — [Proposal 4A](../docs/releases/0004a-memory-domain.md) apresentada (revisão 1, aguardando aprovação), [placeholder 4B](../docs/releases/0004b-memory-infrastructure.md) criado.
  - Pergunta em aberto, sinalizada na Proposal: `Identifier` deveria mover de `packages/identity-engine` para `packages/core` (recomendado, não decidido) — Memory Engine precisa do mesmo mecanismo de Value Object de identificador.
  - Nenhum código do Memory Engine escrito — push do commit desta rodada aguardando confirmação.
- **Revisão da Release 4A pelo Product Owner: APPROVED, nota 10/10** — "a decisão arquitetural mais importante desde o Envelope". Push do commit `5596372` autorizado e realizado. Pedida uma evolução do modelo antes do código (não uma correção — uma extensão), seguida de **"pode seguir"**:
  - **`ContextMemory`** — quarta entidade, estágio bruto/efêmero antes de qualquer `MemoryRecord` (pipeline `Conversation→ContextMemory→MemoryRecord→KnowledgeRecord→DigitalTwin`). → [ADR-0083](../docs/adr/0083-contextmemory-como-estagio-pre-memory.md)
  - **`confidence`** em `MemoryRecord` — gate independente de promoção, além da estrutura de repetição/generalização já fixada em ADR-0081. Pisos definidos: `0.50` criação, `0.70` Operational→Project, `0.90` Project→LongTerm. → [ADR-0084](../docs/adr/0084-confidence-como-gate-de-promocao.md)
  - **Digital Twin estritamente Event-Driven** — inclusive a primeira população, sem exceção (`Evento → Projection → Twin`, sempre). → [ADR-0085](../docs/adr/0085-digital-twin-estritamente-event-driven.md)
  - **`KnowledgeRecord` imutável e versionado** — `version`/`previousVersionId`, git-like, nunca editado em lugar. → [ADR-0086](../docs/adr/0086-knowledgerecord-imutavel-e-versionado.md)
  - **`origin`** em `MemoryRecord`/`ContextMemory` — string aberta (Telegram/GitHub/Meeting/Email/WhatsApp/Claude/ChatGPT/Gemini/Manual, entre outros), não enum fechado. → [ADR-0087](../docs/adr/0087-memoryrecord-origin-e-candidatura-a-knowledge.md)
  - **Tensão real identificada e resolvida, não decidida em silêncio**: o pipeline do Product Owner lido literalmente sugeria promoção automática de `MemoryRecord` `LongTerm` a `KnowledgeRecord` — conflitava com a curadoria humana já fixada para Knowledge (DOMAIN.md/MEMORY_ARCHITECTURE.md/MEMORY_MODEL.md revisão 1). Resolvido como **candidatura**: `MemoryPromoted` (`toLevel: LongTerm`) sinaliza, nunca cria — transformação em `KnowledgeRecord` real exige ação humana via `/knowledge`, revisada por quem tem a Permission `knowledge.curate`. Mesma ADR-0087.
  - **Exigência única do Product Owner antes do código**: [MEMORY_PROMOTION_RULES.md](../MEMORY_PROMOTION_RULES.md) — respondidas as oito perguntas (sobe/desce/expira/vira Knowledge/vira Twin/quem promove/quem impede/quem revisa). "Quando desce" exigiu um atributo novo não previsto na revisão 1 (`status: Active/Deprecated/Retracted` em `MemoryRecord`, contradição automática vs. retração humana) e políticas de expiração por entidade (`ContextMemory` 30 dias, `MemoryRecord Operational` não-promovido 90 dias, `Project`/`LongTerm` não expiram por tempo). → [ADR-0088](../docs/adr/0088-retracao-expiracao-e-governanca-de-promocao.md)
  - **Decisão de longo prazo formalizada**: "Processo Oficial de Desenvolvimento de Engines do SIGMA" — `Research → Manifesto → Model → Lifecycle → Events → Contract → Proposal → Implementation → Validation → Review → Push`, obrigatório para todo Engine a partir da Release 4. → [ADR-0082](../docs/adr/0082-processo-oficial-de-desenvolvimento-de-engines.md), documentado também em `CONTRIBUTING.md`.
  - **Compromisso formal do Product Owner até a Release 24**: papel de time de revisão de engenharia interno em toda entrega futura, toda decisão justificada pensando no SIGMA daqui a cinco anos — registrado em memória externa (`project_sigma_overview.md`/`feedback_sigma_disciplina_arquitetural.md`), não é uma decisão de arquitetura, não gera ADR.
  - `MEMORY_MODEL.md`/`MEMORY_LIFECYCLE.md` revisão 2, `MEMORY_PROMOTION_RULES.md` novo, sete ADRs novas (0082-0088), `docs/releases/0004a-memory-domain.md` revisão 2, `DOMAIN_EVENTS.md`/`EVENT_CATALOG.md` com cinco eventos novos (`ContextMemoryStarted`/`ContextMemoryClosed`/`MemoryDeprecated`/`MemoryRetracted`/`MemorySubjectPinned`, total onze), `contracts/Memory.contract.yaml` atualizado (quatro tipos de output, onze eventos, três Permissions novas no vocabulário), `CONTRIBUTING.md` com o Processo Oficial documentado.
  - Link-check (216 arquivos `.md`, 0 links quebrados) e ADR-check (88 ADRs, 0 referências inexistentes) — limpos.
  - Nenhum código do Memory Engine escrito ainda — Proposal 4A revisão 2 segue aguardando aprovação antes de qualquer Implementation. Push do commit desta rodada aguardando confirmação separada.
- **"aprovado e pode seguir"** — Proposal 4A revisão 2 aprovada; Implementation da Release 4A executada na mesma rodada.
  - `packages/memory-engine/src/Domain/` completo: `Identifier` (cópia própria — ver decisão abaixo) + 7 Value Objects de identificador, 4 enums, `DistilledFact` (Value Object de suporte), os quatro Aggregates (`ContextMemory`/`MemoryRecord`/`KnowledgeRecord`/`DigitalTwin`), `RecordsDomainEvents`, 12 classes de evento.
  - **35 testes automatizados, 103 assertions, 100% passando** — cobrindo destilação, os dois gates de promoção (estrutura + `confidence`) independentes, contradição/reativação/retração, versionamento de Knowledge, staleness de Twin, e um cenário de engajamento completo ponta a ponta.
  - **Suíte completa do monorepo revalidada — 170 testes**, todos passando (15 skipped em identity-engine/auth por infraestrutura indisponível nesta sessão, pré-existente).
  - **Decisão real tomada durante a Implementation, não decidida silenciosamente**: a pergunta em aberto sobre mover `Identifier` para `packages/core` (sinalizada na Proposal 4A) não foi endereçada na aprovação — escolhida a opção de menor risco (Memory Engine ganha sua própria cópia, Identity Engine não é tocado), consolidação continua recomendada e sinalizada em `NEXT.md`, não descartada.
  - **`TenantId`/`WorkspaceId`/`MissionId` são referências opacas próprias do Memory Engine** — nunca os tipos do Identity Engine, mesmo padrão DDD de bounded context cunhando seu próprio identificador para referência cross-Engine.
  - **Achado real durante a Implementation, corrigido antes do código correspondente**: `MEMORY_PROMOTION_RULES.md` já previa "Deprecated volta a Active", mas nenhum evento existia para essa transição — catalogado `MemoryReactivated` (`memory.reactivated`) em `DOMAIN_EVENTS.md`/`EVENT_CATALOG.md`/`contracts/Memory.contract.yaml` antes de escrever a classe de evento, mesma disciplina de sempre aplicada dentro da própria Implementation.
  - **`MemorySubjectPinned` catalogado mas sem Aggregate próprio nesta sub-Release** — sinalizado explicitamente em vez de forçar uma modelagem artificial não prevista em MEMORY_MODEL.md; decisão de onde persistir fica para a Release 4B.
  - `evaluatePromotionToLongTerm()` recebe a `subjectKey` generalizada já calculada pelo chamador — o novo `MemoryRecord` LongTerm é criado com essa chave generalizada, não com a original, consequência direta de ADR-0081.
  - [Decision Log](../docs/releases/0004a-memory-domain-decision-log.md) e [Validation Report](../docs/releases/0004a-memory-domain-validation-report.md) publicados — Proposal 4A marcada como aprovada e implementada.
  - Push do commit desta rodada aguardando confirmação separada.
- **"pode seguir"** — Proposal da Release 4B — Memory Infrastructure escrita, substituindo o placeholder anterior.
  - **Achado real, mudando o escopo do que o placeholder previa**: `services/event-bus`'s `RedisEventBus` publica de fato no canal Redis, mas `subscribe()` só entrega dentro do mesmo processo (compõe `InMemoryEventBus` internamente, [ADR-0057](../docs/adr/0057-eventbus-composicao-inmemory.md)) — não existe, em lugar nenhum do projeto, um listener Redis cross-processo real. Já era uma limitação conhecida desde a Release 2 ("fica para quando houver um consumidor de verdade") — `UserTwin` sincronizado a partir de `services/auth` é esse consumidor, e roda num processo/container diferente por definição.
  - A Proposal 4B passa a incluir, como escopo central (não um detalhe): `services/event-bus/src/RedisSubscriber.php` (`pubSubLoop` bloqueante) e `services/memory-worker` (novo serviço deployável, sem HTTP, só o Módulo do Memory Engine + o listener).
  - **Decisão deliberada de escopo**: nenhuma API HTTP pública para o Memory Engine nesta Release — não existe consumidor real (Mission/Planner/Agent ainda não existem); persistência provada via consulta direta ao repositório, não via `curl`.
  - **Decisão deliberada de escopo**: os três algoritmos de raciocínio (destilação de `ContextMemory`, detecção de contradição, generalização de `subjectKey`) continuam não implementados — exigem capacidade de linguagem natural que nenhum Engine tem ainda; `Application/` recebe esses resultados já prontos de quem chama, nunca inventa um heurístico raso só para preencher escopo.
  - **`PinnedMemorySubject` resolvido** — Value Object de `Application/` (não `Domain/`), fechando a pendência explícita deixada pela 4A.
  - Proposal ainda não aprovada — nenhum código de `Application/Infrastructure/Interfaces` do Memory Engine escrito.
- **"siga"** — Proposal 4B aprovada; Implementation completa executada na mesma rodada. **Release 4 — Memory Engine agora COMPLETA (4A + 4B).**
  - `packages/memory-engine/src/Application/`: 5 interfaces de repositório, `PinnedMemorySubject` (Value Object, não Aggregate — fecha pendência da 4A), 13 casos de uso.
  - `packages/memory-engine/src/Infrastructure/`: `MigrationRunner`+`CreateSchema` (5 tabelas + 1 de junção), 5 repositórios `Pdo*`, `KnowledgeFolderIndexer` (indexação real de `/knowledge`).
  - `packages/memory-engine/src/Interfaces/MemoryEngineModule` — segundo `IModule` de domínio real do SIGMA.
  - **O achado mais importante desta Release**: `RedisEventBus`/`InMemoryEventBus` nunca implementaram entrega cross-processo real de eventos (só local, mesmo processo) — lacuna sinalizada desde a Release 2, formalizada em [ADR-0057](../docs/adr/0057-eventbus-composicao-inmemory.md) ("fica para quando houver um consumidor de verdade"). `UserTwin` sincronizado a partir de `services/auth` é esse consumidor. Resolvido com `services/event-bus/src/RedisSubscriber.php` (`pubSubLoop` bloqueante) + `RedisEventBus::dispatchLocally()` + `services/memory-worker` (novo serviço, sem HTTP).
  - **Segundo achado real, durante a validação via Docker**: `memory-worker` morria sozinho após ~60s ocioso — `Predis\Client` sem `read_write_timeout` explícito usa o timeout padrão de socket do PHP, fatal para uma conexão bloqueada em `pubSubLoop()` esperando mensagens que podem não chegar por minutos. Corrigido com `read_write_timeout: -1` na conexão dedicada do worker. Sem este ajuste, o worker seria inutilizável em produção — só apareceu testando a arquitetura de verdade via `docker compose up`, não em teste automatizado.
  - **`docker compose up --build` real, dois containers distintos** (`auth` publicando, `memory-worker` consumindo) — login real → `identity.created`/`workspace.selected` publicados no Redis → `UserTwin` persistido de fato na MariaDB, provado por consulta direta (`identityId`/`userId`/`workspaceId` todos corretos). Restart do `memory-worker` não perdeu o Twin já persistido.
  - **Refinamento real**: `ProjectDigitalTwinFromEvent` usa `identityId` como `externalRef` do `UserTwin`, não `userId` como MEMORY_MODEL.md revisão 2 dizia literalmente — `workspace.selected` só carrega `identityId`, nunca `userId`; os dois são 1:1 e igualmente internos ao SIGMA, a troca preserva o espírito da decisão original.
  - **Decisão deliberada de escopo, não um esquecimento**: nenhuma API HTTP pública para o Memory Engine nesta Release — sem consumidor real ainda (Mission/Planner não existem). Persistência provada via consulta direta ao repositório/MariaDB.
  - **195 testes automatizados no monorepo** (60 novos desta rodada), todos passando — rodados também contra MariaDB real via Docker (0 skips nessa configuração, banco de teste `sigma_memory_test` isolado do `sigma_identity_test` do Identity Engine).
  - `packages/memory-engine/VERSION.md` (novo, SemVer formalizado) e entrada "Release 4 — Memory" em `CHANGELOG.md`.
  - [Decision Log](../docs/releases/0004b-memory-infrastructure-decision-log.md) e [Validation Report](../docs/releases/0004b-memory-infrastructure-validation-report.md) publicados — Proposal 4B marcada como aprovada e implementada.
  - Push do(s) commit(s) desta rodada aguardando confirmação separada.
- **"pode seguir"** — em resposta, perguntei diretamente ao Product Owner qual seria a próxima Release (pendência real, não decidida por mim: Planner×Intent seguia em aberto desde a Release 3.5). Resposta extensa e decisiva:
  - **Roadmap revisado**: nova Release **4.5 — Platform Validation** inserida entre Memory (4) e Mission (5) — não é Release funcional, é engenharia: provar que Identity+Memory+Redis+Docker+Gateway+Worker funcionam juntos sob volume/falha, antes de a Mission Engine passar a depender de tudo isso funcionando de verdade. Dez verificações: stress test (1000 eventos), restart de containers, perda do Redis, perda do Worker, múltiplos usuários, Memory Promotion em volume, Twin Sync, latência, event replay, benchmark.
  - **Planner × Intent (numeração 6/7) ENCERRADO**: mantido [ADR-0031](../docs/adr/0031-ordem-runtime-vs-desenvolvimento.md) sem alteração — "estamos falando de ordem de desenvolvimento, não de ordem lógica de execução [...] não vejo benefício em reabrir essa ADR agora." `6 — Planner`, `7 — Intent`, definitivo.
  - **Visão para a Release 5 (Mission)**: "até hoje o SIGMA guarda, autentica, sincroniza — mas ainda não decide. Mission será a primeira Engine que fará o SIGMA pensar." `Mission` deve ser Aggregate Root (ciclo de vida, eventos, estado, histórico, autonomia, aprovação, retries, compensações, correlação), não um serviço. Processo exigido: 5A Mission Research (Manifesto/Model/Lifecycle/Events/Contract/ADRs/Proposal, sem código) → Review → 5B Implementation → Review → Push — mesmo padrão de Identity/Memory.
  - `ROADMAP.md` atualizado: tabela de status (4A/4B → ✅ Concluída, 4.5 inserida), seção "Release 4 — Memory Engine ✅" resumindo o que foi de fato entregue, nova seção "Release 4.5 — Platform Validation", "Release 5 — Mission Engine" expandida com a visão de Aggregate Root e o processo 5A/5B, nota de pendência Planner/Intent substituída por nota de decisão encerrada.
  - **Proposal da Release 4.5** escrita (`docs/releases/0004.5-platform-validation.md`) — dez itens do Escopo, cada um com critério de sucesso; antecipa explicitamente que os itens 4 (perda do Worker) e 9 (event replay) devem revelar uma limitação real e esperada (Redis pub/sub não persiste mensagem para assinante ausente, diferente de Streams) — não uma falha a esconder, uma característica a documentar e decidir se/quando endereçar.
  - Proposal 4.5 aguardando aprovação — nenhuma das dez verificações executada ainda. Push do(s) commit(s) pendentes aguardando confirmação separada.
- **"sim pode ser"** — Proposal 4.5 aprovada, as dez verificações executadas de fato contra `docker compose up --build` real (`down -v` antes, ambiente genuinamente limpo).
  - **Achado real de maior prioridade, não previsto na Proposal**: nenhum serviço do `docker-compose.yml` declara `restart:` policy. Descoberto ao executar o item 2 (restart de containers) em sequência com o item 3 (perda do Redis) — `memory-worker` morreu (conexão Redis bloqueada em `pubSubLoop()` derrubada) e **não voltou sozinho**, precisou de restart manual. `auth`/`gateway` sobrevivem sozinhos a uma queda de Redis (sem conexão bloqueante); `memory-worker` não, mesmo com o fix de `read_write_timeout: -1` da Release 4B (que resolve timeout de inatividade, não interrupção real de conexão).
  - **Achado esperado, confirmado com evidência concreta**: um usuário real fez login+seleção de Workspace novo via HTTP (`services/auth`) com `memory-worker` já fora do ar — as chamadas HTTP tiveram sucesso (`200`, Redis aceita `PUBLISH` mesmo sem assinante), mas depois de religar o worker, o `UserTwin` **nunca** refletiu o Workspace novo — mensagem perdida para sempre. Responde também o item "Event Replay": não existe nenhum mecanismo de recuperar isso, confirmado, não implementado (decisão deliberada da Proposal).
  - **Resultados fortes, com evidência real**: stress test de 1000 eventos, 0 perdidos (worker de pé), ~112 eventos/s de processamento, ~7.168/s de publicação; latência p50 ~6ms/p95 ~12ms/p99 ~23ms; 20 usuários concorrentes (logins + seleção de Workspace simultâneos via HTTP real), 0 falhas, 0 contaminação cruzada entre `UserTwin`s; Memory Promotion em volume (200 registros reais, MariaDB real) com contagem de promoção exata nos dois saltos; Twin Sync sequencial (4 trocas de Workspace) sempre convergindo para o último evento.
  - Recomendação registrada, não implementada nesta Release: `restart: on-failure`/`unless-stopped` em todo `docker-compose.yml` (baixo risco); migrar `RedisSubscriber` de Redis pub/sub para Redis Streams (persistência/replay real) como candidata a Release de hardening futura.
  - [Decision Log](../docs/releases/0004.5-platform-validation-decision-log.md) e [Validation Report](../docs/releases/0004.5-platform-validation-validation-report.md) publicados, com benchmark de referência. `ROADMAP.md` atualizado (4.5 → ✅ Concluída).
  - Push do(s) commit(s) pendentes aguardando confirmação separada.
- **"continue"** — perguntei diretamente ao Product Owner qual seria a próxima Release (não decidi sozinho, dado o histórico de perguntas em aberto). Resposta extensa: revisão do roadmap (Release 4.5 confirmada, ordem Planner/Intent encerrada definitivamente mantendo ADR-0031) e visão detalhada para a Release 5 — Mission Engine tratada como Aggregate Root, com o processo explícito "5A Mission Research (sem código) → Review → 5B Implementation → Review → Push". Iniciei a Release 5A: pesquisa dedicada (agente de exploração) sobre toda menção existente a "Mission" no repositório antes de escrever qualquer modelo.
  - **Achado real de pesquisa, contradição estrutural**: `packages/README.md`/`packages/mission-engine/README.md` listavam `mission-engine` dependendo de `planner-engine` (e `planner-engine` de `intent-engine`) — contradizia diretamente [ADR-0031](../docs/adr/0031-ordem-runtime-vs-desenvolvimento.md) (Mission construída antes de Planner, que é construído antes de Intent). Corrigido; formalizado como decisão permanente em [ADR-0092](../docs/adr/0092-plan-e-conceito-proprio-do-mission-engine.md) — `Plan`/Subtask candidata são Value Objects do próprio Mission Engine, nunca dependência de código do Planner Engine.
  - **Decisão real: onde o domínio de Mission começa** — `ARCHITECTURE.md §6` misturava estágios de Intent/Planner (antes de qualquer Mission existir) com o que é de fato Mission Engine. Resolvido: o Aggregate `Mission` só existe a partir de um `Plan` aceito — nunca antes. [ADR-0089](../docs/adr/0089-mission-nasce-do-plan-nao-da-intent.md).
  - **Decisão real: aprovação como estado de primeira classe** (`PendingApproval`), complementar ao gate por Capability já existente em `SIGMA_PROTOCOL.md §5`, nunca substituto. [ADR-0090](../docs/adr/0090-aprovacao-como-estado-de-primeira-classe.md).
  - **Decisão real: retry é histórico de Subtask; compensação é estado da Mission** — uma Mission com Subtask tentando de novo continua `InProgress`; só falha definitiva com efeito já produzido move a Mission inteira para `Compensating`, que sempre termina em `Failed`. [ADR-0091](../docs/adr/0091-retry-subtask-compensacao-mission.md).
  - **Decisão real: `Mission.workspaceId` é opcional** — Missions de sistema/manutenção não precisam de Workspace. [ADR-0093](../docs/adr/0093-mission-workspace-opcional.md).
  - **Reconciliação de três vocabulários pré-existentes do mesmo fluxo**: ADR-0003 (prosa, 9 passos), `ARCHITECTURE.md §6` (diagrama, 11 estados) e `EVENT_MODEL.md` (12 eventos canônicos) nunca tinham sido normalizados — `MISSION_LIFECYCLE.md` assume esse papel explicitamente, sem reescrever nenhum dos três.
  - Duas divergências cosméticas sinalizadas, não corrigidas retroativamente em ADR (ADRs não são revogadas por edição): ADR-0003 cita `ARCHITECTURE.md §5` para o ciclo de vida (correto é §6); `DOMAIN.md` citava `SIGMA_PROTOCOL.md#4-autonomia-progressiva` (correto é #5) — este último corrigido diretamente, por não ser ADR.
  - **Entregue**: [MISSION_MANIFESTO.md](../MISSION_MANIFESTO.md), [MISSION_MODEL.md](../MISSION_MODEL.md), [MISSION_LIFECYCLE.md](../MISSION_LIFECYCLE.md), [MISSION_EVENTS.md](../MISSION_EVENTS.md) (doze eventos Technical — três já catalogados desde a Foundation sem dono real, nove novos), `contracts/Mission.contract.yaml`, cinco ADRs (0089-0093), [Proposal 5A](../docs/releases/0005a-mission-research.md) e seu [Decision Log](../docs/releases/0005a-mission-research-decision-log.md). `EVENT_MODEL.md`/`EVENT_CATALOG.md` atualizados com os eventos novos. `ROADMAP.md` atualizado (5A/5B substituindo a linha única "5").
  - Nenhum código do Mission Engine escrito ainda — Proposal 5A aguardando aprovação. Push do(s) commit(s) pendentes aguardando confirmação separada.
