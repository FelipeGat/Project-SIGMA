# SO
.DS_Store
Thumbs.db
desktop.ini

# Editores
.idea/
.vscode/*
!.vscode/extensions.json

# PHP / Composer — packages/*, services/* (monorepo, cada um com seu proprio composer.json)
vendor/
composer.phar
.phpunit.result.cache
.phpunit.cache/

# Node (apps/web, apps/mobile — a partir da Release 11)
node_modules/
dist/
.expo/

# Variaveis de ambiente — nunca commitadas, .env.example sim
.env
.env.*
!.env.example
!**/.env.example

# Laravel (quando services/gateway ganhar o framework completo)
storage/*.key
bootstrap/cache/*
public/hot
public/build

# Logs e artefatos locais
*.log
