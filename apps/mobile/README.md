# apps/mobile

App mobile do SIGMA — React Native + Expo. Consome `services/gateway` e os componentes de `packages/design-system`; nenhuma lógica de domínio duplicada no cliente.

Vazio propositalmente durante a Fase Foundation. Nenhum código de aplicação é criado até que a Release 11 — Interfaces seja apresentada e aprovada (ver [ROADMAP.md](../../ROADMAP.md)).
