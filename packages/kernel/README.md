# packages/kernel

Implementação do Kernel do SIGMA: bootstrap, configuração por ambiente, registro e ciclo de vida dos Engines, carregamento do Plugin System, contexto de execução (Tenant/Workspace/User atual), health-check. Ver [KERNEL.md](../../KERNEL.md) para o que pertence e o que nunca pertence a este pacote.

Vazio na Fase Foundation. Primeiro pacote a ser implementado — Release 2 do [ROADMAP.md](../../ROADMAP.md), pré-requisito de todos os demais.
